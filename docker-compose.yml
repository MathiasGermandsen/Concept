services:
  backend:
    build: .
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    env_file:
      - .env
    # depends_on:
    #   postgres:
    #     condition: service_healthy
    restart: unless-stopped

#   postgres:
#     image: postgres:17-alpine
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: mobiledisco
#     ports:
#       - "5432:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U postgres"]
#       interval: 5s
#       timeout: 3s
#       retries: 5
#     restart: unless-stopped

# volumes:
#   postgres_data:
